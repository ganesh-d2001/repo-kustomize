# PostgreSQL StatefulSet
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql-db-service-test
  namespace: test
spec:
  serviceName: postgresql-db-service-test
  selector:
    matchLabels:
      app: postgresql-db-service-test
  replicas: 1
  template:
    metadata:
      labels:
        app: postgresql-db-service-test
    spec:
      terminationGracePeriodSeconds: 10
      containers:
      - name: postgresql-db-service-test
        image: postgres:latest
        ports:
        - containerPort: 5432
          name: post-ser
        volumeMounts:
        - name: postgresql-db-pvc-test
          mountPath: /data
        env:
        - name: POSTGRES_PASSWORD
          value: Test@123
        - name: PGDATA
          value: /data/pgdata
  volumeClaimTemplates:
  - metadata:
      name: postgresql-db-pvc-test
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: local-storage
      resources:
        requests:
          storage: 1Gi