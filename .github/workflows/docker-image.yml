name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    # Set up SSH to access the remote server
    - name: Set up SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
        chmod 600 ~/.ssh/id_ed25519
        ssh-keyscan 34.135.120.96 >> ~/.ssh/known_hosts

    # Build the Docker image
    - name: Build the Docker image
      run: |
        docker build -t prod_data:1 .

    # Save the Docker image as a tarball file
    - name: Save Docker image to tarball
      run: |
        docker save prod_data:1 -o prod_data_image.tar

    # Copy the tarball to the remote server via SSH
    - name: Copy image to remote server
      run: |
        scp prod_data_image.tar ganeshd@34.135.120.96:/home/ganeshd/tarfiles/

    # Load the Docker image on the remote server and run it
    - name: Load and run Docker image on remote server
      run: |
        ssh prod@34.135.120.96 << 'EOF'
          docker load -i /home/ganeshd/tarfiles/prod_data_image.tar
          docker run -d --name prod_container -p 80:80 prod_data:1
        EOF
